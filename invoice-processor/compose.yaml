services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=invoice_db'
      - 'POSTGRES_PASSWORD=invoiceprocessor'
      - 'POSTGRES_USER=donald'
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
 postgres_data:
